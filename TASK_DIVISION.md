# Pembagian Tugas (3 Orang) — Sprint 5x5 Hari

| Sprint | Hari | Orang A (Customer) | Orang B (Admin) | Orang C (Driver/Worker) | Shared |
|--------|------|--------------------|-----------------|--------------------------|--------|
| **1** | 1-5 | Landing page (navbar, hero carousel, footer) + Register (email + Google OAuth) + Login + redirect middleware + JWT issuing | Admin authorization + role-based middleware (super admin / outlet admin) + user management CRUD + paginated table | Attendance (clock-in/out API, absen, history, outlet admin report) | Monorepo setup + Neon DB project + Prisma schema + migrate + shared types |
| **2** | 6-10 | Verifikasi email (send email + set password, 1hr token expiry) + reset password + edit profile + upload/validate foto (jpg/png/gif, max 1MB) | Outlet CRUD (name, address, coordinates, service radius) + assign user ke outlet + map view (leaflet) | **Driver management** (dashboard, list pickup/delivery request, notifikasi real-time, proses 1 order aktif) + **Socket.IO server setup** (emit event ke driver/worker rooms) | — |
| **3** | 11-15 | Manajemen alamat (CRUD, set main, geocode ke coordinates, map picker UI) | Order management (lihat semua order, filter per outlet/status/tanggal, tracking by status/worker/date) | Worker management (notifikasi via socket, daftar pesanan pending per station type: washing/ironing/packing) | — |
| **4** | 16-20 | Request pickup (pilih alamat & jadwal, cari outlet terdekat within service radius, assign ke driver queue) | Create order dari pickup request (input kg & item quantities, generate invoice) + bypass process (worker request → outlet admin approve/reject with PIN) | **Worker processing** (input ulang quantity, validasi mismatch vs station sebelumnya, request bypass) + **Driver processing** (update status: pickup → on the way → arrived, single-order lock, pickup/delivery history) | — |
| **5** | 21-25 | Order list (pagination, search by date/order no, realtime tracking via socket) + Midtrans payment + auto-confirmation 2×24h + complaint submission (damaged/missing) + order confirmation UI | Sales report (income per hari/bulan/tahun, filter outlet + date, export PDF/CSV) + Employee performance report (total jobs per worker/driver, filter outlet + date) | Packing station (cek payment → "Siap Diantar" / "Menunggu Pembayaran") + auto trigger delivery request + worker job history + responsive polish UI | Input sanitization (XSS) + rate limiting + security headers + final QA + code cleanup + deploy backend (Railway) + deploy frontend (Vercel) |
